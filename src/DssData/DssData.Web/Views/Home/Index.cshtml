@using System.Web
@using System.Configuration

@{
    Layout = null;
}

<!DOCTYPE html>
<html>
	<head>
	    <title>Login</title>
		 
	    
	    @Styles.Render("~/Content/App.css")
	    @Styles.Render("~/Content/bootstrap.css")
	    @Styles.Render("~/Content/bootstrap.css.map")
	    @Styles.Render("~/Content/bootstrap.min.css")
	    @Styles.Render("~/Content/bootstrap-theme.css")
	    @Styles.Render("~/Content/bootstrap-theme.min.css")
	    @Styles.Render("~/Content/bootstrap-theme.min.css.map")

		@Scripts.Render("~/Scripts/jquery-3.1.1.js")
	    @Scripts.Render("~/Scripts/jquery-3.1.1.min.js")
	    @Scripts.Render("~/Scripts/bootstrap.min.js")
	    @Scripts.Render("~/Scripts/jquery-3.1.1.min.js")
	    @Scripts.Render("~/Scripts/bootstrap.js")



	</head>
	<body class="login-body">
		<div class="col-xs-12 col-sm-10 offset-sm-2 col-md-8 offset-md-4"">
		<section id="login-form-container">	
				<h2 class="login-header">Dss Data Login</h2>
                @using (Html.BeginForm("Dashboard", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form", id="login-form"}))
                {
                    @Html.AntiForgeryToken()
                    <h4 class="login-header">Use a local account to log in.</h4>
                    <hr />
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group email">
                        
                        @Html.Label("Username", new { @class = "col-sm-2 col-md-2 control-label"})
                        <div class="col-sm-10 col-md-8">
							@Html.TextBox("Username", "", new {@class = "form-control", id="login-email"})
                            
                        </div>
                    </div>
                    <div class="form-group">
                        @Html.Label("Password", new { @class = "col-sm-2 col-md-2 control-label" })
                        <div class="col-sm-10 col-md-8">
                            @Html.Password("Password", "",new { @class = "form-control", id="login-password" } ) <br />

                            <button type="submits" id="login-button" class="btn btn-success pull-right">Login</button>
                        </div>
					
					
                    </div>
                    <div class="form-group">
                        <div>
                             
                        </div>
                    </div>
                }
            </section>
			</div>
	</body>
</html>
<script>
	$(document).ready(function(){
		<!--$("#login-button").click(function(){
			var email = $("#login-email").val();
			var password = $("#login-password").val();
			$.ajax({
				url:'@Url.Action("Authenticate","Account")',
				type:'POST',
				data: {'email':email,'password':password},
				dataType:"json",
				success: LoadDashboard,
				error: function (response) {
					alert(response.Message);
				}
			});
		});-->
	
	});
	function LoadDashboard(response)
	{
		if(response.Success)
		{
			$("#login-form").submit();
		}
		else
		{
			alert(response.Message)
		}
	}
</script>